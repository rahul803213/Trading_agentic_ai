cat > .env.example << 'EOF'
# Database
DATABASE_URL=postgresql://agent_user:agent_pass@localhost:5432/agent_db

# LLM
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# FastAPI
ENVIRONMENT=development
DEBUG=true
EOF

# Copy to .env (Git will ignore .env but track .env.example)
cp .env.example .env